<html>
    <head>
        <title>Is JStyles Being Lazy?</title>
        <meta name="description" content="This website tracks JStyles' daily activity patterns, specifically focusing on time spent in bed versus being productive.">
            <link rel="shortcut icon" type="image/png" href="/static/icon.png"/>
            <link rel="apple-touch-icon" href="/static/icon.png">
            <link rel="icon" type="image/png" href="/static/icon.png">
            <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>
            {%- if sleep -%}
                😴
            {%- elif in_bed -%}
                🛏️
            {%- else -%}
                💪
            {%- endif -%}
        </text></svg>">
        <style>
                      :root {
                --background-color: #f5f5f5;
                --container-background: white;
                --text-color: #333;
                --heading-color: #2c3e50;
                --status-background: #ecf0f1;
                --time-background: #f8f9fa;
                --link-color: #3498db;
                --box-shadow: 0 4px 6px rgba(0,0,0,0.1);
                --time-border: #3498db;
            }

            /* Dark mode */
            @media (prefers-color-scheme: dark) {
                :root {
                    --background-color: #1a1a1a;
                    --container-background: #2d2d2d;
                    --text-color: #e0e0e0;
                    --heading-color: #9cc4e4;
                    --status-background: #3d3d3d;
                    --time-background: #363636;
                    --link-color: #61dafb;
                    --box-shadow: 0 4px 6px rgba(0,0,0,0.3);
                    --time-border: #61dafb;
                }
            }

            body {
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                margin: 0;
                padding: 40px;
                background: var(--background-color);
                color: var(--text-color);
            }
            .container {
                max-width: 800px;
                margin: 0 auto;
                background: var(--container-background);
                padding: 40px;
                border-radius: 15px;
                box-shadow: var(--box-shadow);
            }
            h1 {
                text-align: center;
                color: var(--heading-color);
                font-size: 36px;
                margin-bottom: 40px;
            }
            .status {
                font-size: 28px;
                font-weight: bold;
                margin: 30px 0;
                padding: 20px;
                border-radius: 10px;
                text-align: center;
                background: var(--status-background);
                position: relative;
            }
            .status .tooltip {
                font-size: 12px;
                visibility: hidden;
                width: 200px;
                background-color: var(--container-background);
                color: var(--text-color);
                text-align: center;
                border-radius: 6px;
                padding: 5px;
                position: absolute;
                z-index: 1;
                bottom: 125%;
                left: 50%;
                margin-left: -100px;
                opacity: 0;
                transition: opacity 0.3s;
            }
            .status:hover .tooltip {
                visibility: visible;
                opacity: 1;
            }
            .time {
                font-size: 20px;
                margin: 20px 0;
                padding: 15px;
                border-left: 4px solid var(--time-border);
                background: var(--time-background);
            }
            .nav-links {
                text-align: center;
                margin: 20px 0;
            }
            .nav-links a {
                color: var(--link-color);
                text-decoration: none;
                margin: 0 10px;
            }
            .nav-links a:hover {
                text-decoration: underline;
            }
            footer {
                text-align: center;
                margin-top: 100px;
                font-size: 12px;
            }
        </style>
    </head>
    <body>
    <script data-goatcounter="https://lazyj.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>
    <noscript>
        <img src="https://lazyj.goatcounter.com/count?p=/">
    </noscript>
    <div class="nav-links">
    <a href="/">Home</a>
    <a href="/about">About</a>
</div>
        <div class="container">
            <h1>Is JStyles Being Lazy?</h1>

            <div class="status">
                Current Status:
                {% if sleep %}
                    Sleeping 😴
                    <span class="tooltip">Between 10pm and 9am and phone is off</span>
                {% elif in_bed %}
                    Being Lazy 🛏️
                {% else %}
                    Being Productive 💪
                {% endif %}
            </div>

            <div class="time">
                Time Being Lazy: {{ time_in_bed }}
            </div>

            <div class="time">
                Last Updated: {{ last_updated }} UTC
            </div>
        </div>
    </body>
    <footer>
    <p>Somes emojis by OpenMoji – the open-source emoji and icon project. License: CC BY-SA 4.0</p>
    </footer>
</html>
